<Window x:Class="StylesSample.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StylesSample"
        mc:Ignorable="d"
        Title="Window1" Height="450" Width="800">
  <Window.Resources>
    <ControlTemplate x:Key="LabelTemplate" TargetType="Label">
      <StackPanel Orientation="Horizontal">
        <Ellipse Width="50" Height="50" Fill="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Background}" Margin="5"/>
        <!--<ContentControl Content="{TemplateBinding Content}" VerticalAlignment="Center" Margin="5"/>-->
        <ContentPresenter VerticalAlignment="Center" Margin="5"/>
        <Ellipse Width="50" Height="50" Fill="{TemplateBinding Foreground}" Margin="5"/>
      </StackPanel>
    </ControlTemplate>

    <Style TargetType="Label">
      <Setter Property="Template" Value="{StaticResource LabelTemplate}"/>
    </Style>
    
  </Window.Resources>
  <Grid>
    <Label Template="{StaticResource LabelTemplate}" Background="Blue" Foreground="Red"  HorizontalAlignment="Left" Margin="111,84,0,0" VerticalAlignment="Top" Content="Label 1"/>
   
    <Label Background="LightGreen" Foreground="DarkGoldenrod" Content="Label" HorizontalAlignment="Left" Margin="382,170,0,0" VerticalAlignment="Top"/>
    <Label Background="DarkOliveGreen" Foreground="Orange" Content="Label" HorizontalAlignment="Left" Margin="175,280,0,0" VerticalAlignment="Top"/>

  </Grid>
</Window>
